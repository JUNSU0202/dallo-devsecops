# Dallo Security Gate 설정 (.github/dallo-gate.yml)
#
# CI/CD 파이프라인에서 빌드를 차단하는 임계값을 정의합니다.
# 프로젝트별로 조정 가능합니다.

# CRITICAL 등급 취약점이 이 수 이상이면 빌드 실패
critical_threshold: 1

# HIGH 등급 취약점이 이 수 이상이면 빌드 실패
high_threshold: 5
